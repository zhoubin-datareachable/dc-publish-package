name: "Publish npm package"
author: "zb"
branding:
  icon: "anchor"
  color: "blue"
description: "public package"
inputs:
  repository:
    description: "仓库名称"
    default: ${{ github.repository }}
  name:
    description: "name"
    required: false
  host:
    description: "host"
    required: false
  token:
    description: "token"
    required: false
  rea:
    description: "rea"
    required: false
  tag:
    description: "tag"
    required: false
runs:
  using: "docker"
  image: "Dockerfile"
